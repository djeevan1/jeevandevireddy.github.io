{
  "name": "event-sync-service",
  "version": "4.1.0",
  "private": true,
  "description": "Reconciles CRM and calendar meeting data into a unified API and web UI.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/reconcile.js && node --check src/server.js && node --check public/app.js && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
